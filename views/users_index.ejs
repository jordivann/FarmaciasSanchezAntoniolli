<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Usuarios</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Custom styles -->
    
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        body {
            background-color: #c4c4c4e0;
            min-height: 110vh;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 30px;
        }
        .table-container {
            min-width:850px;
            max-width: 1020px;
            margin: 0 auto;
        }
        .table {
            background-color: #f1f1f1;
            border-radius: 10px;
            tr{
                text-transform: capitalize;
            }
        }
        .titulo {
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            margin-bottom: 20px;
        }
        .boton:hover{
            text-decoration: none;
            color:#c4c4c4e0;
        }
        .center{
            display:flex;
            align-items:center;
            justify-content: center;
        }
        .logo{
            bottom:0;
            display:flex;
            align-items:center;
            justify-content: center;
            width:100%;
            img{
                width:40%;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/banner', { showBackButton: true, loggedIn: true }) %>
        
    <%- include('partials/nav')%>
    <div class="container">
        <div class="table-container">
            <div class="titulo">
                <h1 class="text-left">Usuarios FSA</h1>
                <a href="/users/new" class="boton "><i class="fas fa-user-plus"></i> Nuevo usuario</a>
            </div>
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr  class="text-center">
                        <th>ID</th>
                        <th >Nombre de Usuario</th>
                        <th >¿Es Administrador?</th>
                        <th >Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(user => { %>
                        <tr  class="text-center">
                            <td><%= user.id %></td>
                            <td><%= user.username %></td>
                            <td><%= user.isadmin ? 'Sí' : 'No' %></td>
                            <td class="center">
                                <a href="/edit_user/<%= user.id %>" class="btn btn-sm btn-primary r">Editar</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
    <div class="logo">
        <img src="./images/Logo azul.png" alt="Logo" class="logo-img">
    </div>
</body>
</html>
